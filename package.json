{
  "name": "gc-trade-journal",
  "version": "1.0.0",
  "private": true,
  "description": "GC · Trade Journal — a daily market-bias journal (static HTML/CSS/JS)",
  "scripts": {
    "test": "node tests/smoke.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
